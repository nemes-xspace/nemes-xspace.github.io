NEMES Miner v0.2.0 (Linux, testnet) — Hizli kurulum (18 Eyl 2026 guncel)

GEREKSINIM: NVIDIA GPU + CUDA surucu (gomme icin). Islemci modunda da
calisir ama yavastir. Ayrica bir embed-api gerekir (adim 2).

1) Kayit:
   curl -s -X POST https://komuta.nemes-x.space/api/kayit \
     -H 'Content-Type: application/json' \
     -d '{"cuzdan":"TRC20_ADRESIN","makine_id":"ev-pc-1"}'
   -> donen "token" alanini kaydet (bir daha gosterilmez).

2) Gomme ucu (SART - madenci metni sayiya ceviremezse uretemez):
   LM Studio'yu kur, nomic-embed-text-v1.5 modelini indirip sunucuyu baslat
   (ornek port 1234), ardindan --embed-api http://127.0.0.1:1234 olarak ver.
   (NEMES gomuntu paketi yakinda bu sayfadan dagitilacak; simdilik LM Studio.)

3) Calistir (ornek - token ve embed-api kendine gore):
   ./nemes-miner mine --simple \
     --komuta https://komuta.nemes-x.space \
     --token TOKEN \
     --embed-api http://127.0.0.1:1234 \
     --kira --kira-adet 2000
   (--kira: gorevleri toplu kiralar, baglantiyi rahatlatir. Ilk surumde sart degil.)

4) Durum: ./nemes-miner status --watch
   Bosluk onerisi (opsiyonel, stake'li): ./nemes-miner bosluklar

Notlar:
- CF korumasi Miner UA ile gecilir (istemci otomatik gonderir).
- Bu paket CLI madencidir (arayuzsuz). Masaustu uygulamasi ayrica duyurulacak.
- Sorun: nemes-x.space@zohomail.eu
